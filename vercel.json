{
  "version": 2,
  "functions": {
    "api/*.ts": { "runtime": "nodejs20.x" }
  },
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
